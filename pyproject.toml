[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ai-usage-monitor"
version = "0.2.0"
description = "CLI usage monitor for Claude and Gemini plan/session limits"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "rich>=13.7",
]

[project.scripts]
ai-usage-monitor = "ai_usage_monitor.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/ai_usage_monitor"]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
